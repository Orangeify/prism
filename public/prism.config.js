// Optional runtime config for Prism frontend.
// Drop this file into `public/` (or edit it) to override the default WebSocket host used by the client.
// Example: set WISP_HOST to the external host running your wisp websocket server.
window.__PRISM__ = {
  // Either provide a full websocket URL like 'wss://example.com/wisp/' or set WISP_HOST to a host (hostname:port)
  // WISP_URL: 'wss://example.com/wisp/',
  // WISP_HOST: 'example.com'
};
